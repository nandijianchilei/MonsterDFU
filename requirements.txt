aiohttp>=3.9
aiosqlite>=0.20
fastapi>=0.100
uvicorn>=0.20
sse-starlette>=1.8
aiohappyeyeballs>=2.3
pyyaml>=6.0
scapy>=2.5
httpx>=0.27
# 安装可选 ML 功能（向量库/Embedding，非核心链路）请执行：pip install .[ml] 或 pip install chromadb sentence-transformers
# aio_pika 已弃用：RabbitMQ 消息总线未真正接入（rabbitmq_bus.py 仅被已废弃 worker 引用），当前为单机多进程本地编排
