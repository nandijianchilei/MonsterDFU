aiohttp>=3.9
aiosqlite>=0.20
fastapi>=0.100
uvicorn>=0.20
sse-starlette>=1.8
aiohappyeyeballs>=2.3
pyyaml>=6.0
scapy>=2.5
httpx>=0.27
# Optional ML features (vector store / embeddings, non-core): run `pip install .[ml]` or `pip install chromadb sentence-transformers`
# NOTE: aio_pika is deprecated - the RabbitMQ message bus is not wired in (rabbitmq_bus.py is only referenced by the obsolete worker). Current runtime uses single-host multi-process local orchestration.
